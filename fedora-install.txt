#########################
NVIDIA DRIVERS
#########################

# Ensure system is up to date

sudo dnf update

# Enable rpm fusion for nvidia drivers

sudo dnf install https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
sudo dnf install https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm

# Install drivers and cuda support

sudo dnf install akmod-nvidia
sudo dnf install xorg-x11-drv-nvidia-cuda

# Verify driver installation

modinfo -F version nvidia

#########################
DESKTOP ENVIRONMENT
#########################

# Add repositories
sudo dnf copr enable solopasha/hyprland
sudo dnf copr enable lihaohong/yazi
sudo dnf copr enable dejan/lazygit
sudo dnf copr enable sneexy/zen-browser

# Install packages

sudo dnf install $(< packages.txt)

# Create symlinks

ln -sfn ~/projects/dotfiles/alacritty ~/.config/alacritty
ln -sfn ~/projects/dotfiles/hypr ~/.config/hypr
ln -sfn ~/projects/dotfiles/lazygit ~/.config/lazygit
ln -sfn ~/projects/dotfiles/nvim ~/.config/nvim
ln -sfn ~/projects/dotfiles/waybar ~/.config/waybar
ln -sfn ~/projects/dotfiles/yazi ~/.config/yazi
